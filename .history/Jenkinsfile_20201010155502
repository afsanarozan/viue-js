// script
node {
    // script here
}

//declaratif

pipeline {

    agent any

    stages {
        stage('build Project') {
            steps{
                echo 'build...'
            }
        }

        stage('Run Testing') {
            step {
                echo 'testing.. '
            }
        }
    }
}